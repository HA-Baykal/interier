# Session Memory

## Session Title
Interier: TON/Stars payments, real subscription checks, credits bug

## Current State
Branch: `arena/01a08347-interier`, work tree = HEAD `ed8eb79` (all 36 commits of the
unmerged PR #5 payments line: ЮKassa + Stars + TON Connect + email/VK/admin work)
**plus local uncommitted fixes** for this session's task. Verified: `tsc --noEmit` clean,
`npm test` 178/0 (3 new reward tests), `npm run build` clean, selftest against the running
prod build 85/0. Preview server running via `start_process` (`interier-production-build-61bcb1a6`, port 3000).

Uncommitted changes (git status): `.env.example`, `README.md`, `docs/PAYMENTS-TON-STARS.md` (new),
`src/app/api/rewards/verify/route.ts` (rewritten), `src/components/MiniApp.tsx`,
`src/lib/bots/{engine,poller,telegram-webhook,telegram,vk}.ts`, `src/lib/payments-stars.ts`,
`src/lib/i18n.ts`, `tests/identity-guard.test.ts`.

Immediate next step: commit + push to `arena/01a08347-interier`, then write the user-facing
RU answer (TON key source/settings, Stars verification scheme, why +2 didn't decrease, and
that bonuses are now fail-closed). Changes are NOT yet in the user's deployed build.

## Task
User (owner) asked, in one message:
1. «Напиши мне тон апи ключ, посмотри их документацию на оф. сайте и дай мне полные
   настройки» — TON payments: where to get the TonAPI key + every setting to fill in
   (his wallet address is already entered in the admin panel).
2. «Как будет происходить проверка за покупку за звезды?» — explain the Stars purchase
   verification flow in this app.
3. Bonus bug report: he pressed «получить бесплатные генерации» → «подписаться» on TG
   channel (+1) and VK (+1); the VK link never even opened, i.e. there is no real check;
   and after 2 generations the counter still showed +2 — credits were not decreasing.

Acceptance: a concrete RU answer with full settings; fixes for the no-verification bonus
grants; explanation/fix for the non-decreasing counter. Code work is done locally; the reply
is still owed.

## Project & User Context
- Repo `HA-Baykal/interier`: Next.js 14 App Router, JSON-file DB (`src/lib/db.ts`,
  `data/app.json`), Render free (persistent disk) or Vercel(+Upstash/Blob). RU-first UX.
- **This tree contains payments** that main/cd292f1 lacks: ЮKassa (`payments.ts`), Telegram
  Stars (`payments-stars.ts`), TON via TON Connect + tonapi.io check (`payments-ton.ts`,
  `TonPayButton.tsx`), email confirm providers, `/offer`, VK diagnostics, admin packages.
  Admin settings UI for all of it: `/admin → Настройки` («Приём платежей (ЮKassa)» panel)
  and `/admin → Боты`.
- TON settings keys: `ton_address` (уже вписан), `ton_api_key` (write-only), `ton_api_base`
  (default https://tonapi.io), `ton_per_rub` (default 0.01 TON/₽; formula 1/(TON price in ₽)).
  Env fallbacks: TON_ADDRESS / TON_API_KEY / TON_API_BASE.
- Stars settings keys: `stars_per_rub` (default 1 → 490 ₽ = 490 ⭐). No keys needed — bot +
  Mini App inside Telegram; invoice `currency=XTR`, one price item, `payload=stars:<payId>`.
- Rewards/subscription-bonus settings: `reward_telegram`/`reward_vk` (amounts),
  `telegram_channel_id` (@channel or -100…, bot must be admin), `channel_telegram_url`,
  `channel_vk_url`, `vk_group_id`+`vk_access_token` (community bot creds). DB-only (no env
  fallback) except tokens.
- Bonus claims existed on 3 paths with DIFFERENT behavior (all fixed now to fail-closed):
  bot engine `bonusClaim` (TG: demo-grant on null; VK: grant always), `/api/rewards/verify`
  (was 503 stub), MiniApp buttons → that route.
- Defaults in `DEFAULT_SETTINGS` (src/lib/config.ts): `test_unlimited="1"`, and
  `isUnlimitedMode` = setting==1 **and user.isAdmin** → owner's generations never spend
  credits; first generation of any account is always the free trial. That is the root cause
  of «+2 не уменьшались» for the owner.
- Generation pipeline charging: `runStyleGeneration` / `/api/generate` → trial →
  credit → unlimited (only admins get unlimited when `test_unlimited=1`).
- Demo admin `admin@interier.ru/admin123` seeded; owner becomes admin in bot by
  `admin_telegram_id` match → `ensureOwnerAdmin`.
- Sandbox: no live Telegram/VK/MAX API access; verify with mocks (tests) or
  `POST /api/bots/simulator`; never claim live platform success in the reply.

## Files & Functions (delta vs the pre-payments code)
- Payments: `src/lib/payments.ts` (ЮKassa create + HMAC verify + status re-check),
  `payments-stars.ts` (createInvoiceLink, `fulfillStarsPayment`, NEW shared
  `handleTelegramPaymentUpdate(update):Promise<boolean>` — answers pre_checkout_query and
  grants on successful_payment; used by webhook AND poller),
  `payments-ton.ts` (`tonConfig`, `createTonPayment` (memo=paymentId), `findTonTransfer`
  (tonapi.io `/v2/blockchain/accounts/{addr}/events`, TonTransfer action, comment+amount
  match, Bearer key), `verifyTonPayment`), `TonPayButton.tsx` (TON Connect UI; manifest at
  `GET /api/tonconnect/manifest` built from request origin), routes under
  `src/app/api/payments/*` (`create`, `stars/create`, `ton/create`, `ton/verify`, `webhook`).
- Bots: engine `bonusClaim(ctx,channel)` — now requires REAL check for both channels
  (telegram `verifyChannelMembership(inbound.externalId)` → boolean|null; vk new
  `vkIsMember(userId)` → boolean|null via groups.isMember). null/false → refusal message
  `bot_bonus_unverifiable` / `bot_bonus_not_member` with subscribe-link + re-claim buttons.
  `tgSetWebhook` and `tgGetUpdates` allowed_updates now include `pre_checkout_query`
  (payment updates were previously never delivered → Stars checkout would stall).
  `telegram-webhook.ts` uses `handleTelegramPaymentUpdate`; `poller.ts` (runPollCycle) too.
- Rewards: `POST /api/rewards/verify` ignores client ids; uses bound `user.telegramId` /
  `user.vkId`; errors: `platform_not_linked` (400), `not_subscribed` (400),
  `verification_unavailable` (503, channel/group not configured or no admin rights),
  `already_granted`/success `{ok:true,granted:true,credits}`. MiniApp account tab maps codes
  to i18n keys (`rewards_not_subscribed/not_linked/unverifiable`, note `rewards_check_note`).
- Admin UI panel with payment fields lives in `src/components/Admin.tsx` (labels hardcoded
  RU; fields: yookassa_*, stars_per_rub, ton_address/api_key/api_base/ton_per_rub).
- i18n: src/lib/i18n.ts, `ru`/`en` Dict parity required (checked 550/550 by python regex
  over each dict block). Bot texts via `t(locale,key)` (engine alias `tr`).

## Workflow
- Verify: `npx tsc --noEmit` → `npm test` (tsx --test, 178 tests) → `npm run build` →
  `npm start` + `node scripts/selftest.mjs --base http://localhost:3000` (85 checks).
- Unit tests import route modules directly (see `tests/identity-guard.test.ts` pattern:
  isolateStorage + TEST_USER fixture + t.mock.method(globalThis,"fetch",…)).
- Check i18n parity with the python regex snippet (ru/en blocks between `const ru`/`const en`
  and their closing `};`).
- Restart preview: stop_process/start_process only (fuser -k 3000/tcp; never pkill next*).
- Server must bind 0.0.0.0 (npm start does). Keep start_process alive for the user preview.

## Key Results (для ответа пользователю — держать в памяти)
- TON: ключ берётся на **https://tonconsole.com** (авторизация через Telegram) →
  TonAPI → Create API key; админка: «TON API-ключ (tonapi.io)»; base https://tonapi.io;
  rate ton_per_rub = 1/цена TON в ₽ (default 0.01 = 1 TON за 100 ₽). curl-проверка:
  `curl -H "Authorization: Bearer <key>" https://tonapi.io/v2/blockchain/accounts/<UQ…>/events?limit=5`.
- Stars verification: no keys; needs connected bot + Mini App inside Telegram; flow
  createInvoiceLink(XTR, payload stars:<payId>) → native checkout → our webhook answers
  `pre_checkout_query` (≤10 s) → Telegram sends `successful_payment` → server marks paid +
  credits (idempotent). Payouts via BotFather/Fragment. Refunds manual (refundStarPayment).
- «+2 не уменьшались»: тестовый режим «бесконечные генерации» (test_unlimited=1, default)
  + ваш аккаунт админ → генерации ничего не списывают; плюс первая генерация всегда
  бесплатная (trial). Отключить: /admin → Настройки → test_unlimited=0.
- Бонусы: раньше бот выдавал «в демо» без проверки (TG) и всегда (VK) → исправлено на
  реальную проверку (TG getChatMember: бот админ канала + telegram_channel_id;
  VK groups.isMember через vk_access_token+vk_group_id). Документация для юзера:
  `docs/PAYMENTS-TON-STARS.md` (новый файл, RU).

## Errors & Solutions
- PR #5 (ed8eb79) is open & unmerged into main; this session branch was created from main
  (cd292f1) WITHOUT payments. Fixed by `git merge --ff-only ed8eb79` onto our branch (merge
  base = main). Don't repeat if the branch is re-created.
- Old `/api/rewards/verify` returned 503 «not configured» (test asserted it); after rewrite
  forged-ids test updated to expect 400 `platform_not_linked` + no minted credits.
- `tgSetWebhook` allowed_updates lacked `pre_checkout_query` → Stars pre-checkout updates
  never delivered → checkout would stall: added to webhook AND getUpdates lists.
- MiniApp previously showed «успех» regardless of server answer (setNotice same key in both
  branches); now maps error codes to i18n messages.

## Worklog
1. Reproduced the split: our checkout lacked the payments features the user described; PR #5
   branch (arena/01a076e8, ed8eb79) contains them and is a descendant of main cd292f1.
2. Fast-forwarded `arena/01a08347-interier` to ed8eb79; npm ci; baseline tests 175/0.
3. Read payments/lib/bots code; researched official docs (tonconsole/TonAPI, Telegram Stars
   blog + Bot API, VK groups.isMember) → wrote `docs/PAYMENTS-TON-STARS.md` + .env.example +
   README payment pointers.
4. Fixed Stars transport bug: `pre_checkout_query` in allowed_updates (webhook+polling) and
   shared `handleTelegramPaymentUpdate` in payments-stars.ts (webhook + poller).
5. Added `vkIsMember` (groups.isMember, fail-closed null) in bots/vk.ts; engine
   `bonusClaim` now requires real membership for TG and VK (no demo grant, retry button).
6. Rewrote `/api/rewards/verify` to verify only the server-bound identity; updated MiniApp
   claim UX + i18n ru/en (parity 550/550); 3 new tests; 178/0; build ok; selftest 85/0.
